-- Main code: hello_world_script.lua

if not pcall(function() loadstring("") end) then
    return -- stops the script if it's not loaded via loadstring
end

-- If it gets here, it's being run correctly through loadstring!
print("Hello World!")
